# 若历史存在无尾斜杠访问，统一到尾斜杠（避免重复内容）
rewrite ^(/\d{4}/\d{2}/\d{2}/[^/]+)$ $1/ permanent;
rewrite ^(/archives)$ $1/ permanent;
rewrite ^(/tags)$ $1/ permanent;
rewrite ^(/page/\d+)$ $1/ permanent;

# 如历史有 /atom.xml，可兼容到 Astro RSS
rewrite ^/atom\.xml$ /rss.xml permanent;
